# 使用结构体组织相关数据

<em>结构体(struct)</em>是一种自定义数据类型，可以把构成有意义群组的多个相关值
打包在一起并分别命名。如果你熟悉面向对象语言，结构体就像对象的数据属性。
本章会比较元组与结构体，在已有知识的基础上说明何时使用结构体能更好地组织数据。

我们会演示如何定义和实例化结构体，并讨论如何定义关联函数，尤其是称为<em>方法
(method)</em>的关联函数，从而指定与结构体类型相关的行为。结构体和枚举（第 6 章
将介绍）是创建程序领域中新类型的基础构件，让你能够充分利用 Rust 的编译期
类型检查。

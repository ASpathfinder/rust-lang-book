# 枚举与模式匹配

本章将介绍<em>枚举(enumeration)</em>，也简称 <em>enum</em>。枚举允许通过列举可能的
变体来定义类型。首先定义并使用枚举，说明枚举如何同时编码含义和数据。接着探索
一个特别实用的枚举 `Option`，它表示一个值可能存在，也可能不存在。然后了解
`match` 表达式中的模式匹配如何针对枚举的不同值轻松运行不同代码。最后介绍
`if let`，这是处理代码中枚举的另一种方便而简洁的惯用写法。
